<nav style="background:#dcdcff;" class="navbar navbar-expand-lg navbar-dark justify-content-between">
  <a style="padding:10px;color:black;font-weight: bold;" class="navbar-brand">ChatApp</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#nav-bar-collapse" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <% if current_user %>
    <div style="padding:10px;color:black;font-weight: bold;">
        <img class="avatar" src="<%= current_user.gravatar_url %>">
        Hai, <%= current_user.username %>
        <span>(<%= link_to 'Logout', destroy_user_session_path, method: :delete %>)</span>
    </div>
  <% end %>
</nav>